# Player

Player is a java server running in a docker container

## prerequisite
    You need to have mave installed 
    sudo apt install maven
    

## Running

    Start the serve by running `./deploy.sh` at root folder

